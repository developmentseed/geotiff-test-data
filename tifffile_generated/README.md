# Test data generated from tifffile

Run `pixi run generate` to create the test data files.

A Python script with the same name generates each file. The scripts use `tifffile` because
rasterio cannot write these layouts. The docstring of each script describes the layout and the
published files that have it.
